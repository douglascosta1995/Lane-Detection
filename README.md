# Lane-Detection

The idea of this project is to create a robot prototype being able to move autonomously following a lane which is randomly set on a flat floor.

Computer Vision (OpenCV library) concept is used.
